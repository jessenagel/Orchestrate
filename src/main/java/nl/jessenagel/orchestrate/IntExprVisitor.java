package nl.jessenagel.orchestrate;

/**
 * Interface for visiting integer expressions in the visitor pattern.
 * It doesn't provide any methods but is included for possible future use and to indicate that an implementing class accepts Int opjects.
 */
public interface IntExprVisitor extends NumExprVisitor {
}
